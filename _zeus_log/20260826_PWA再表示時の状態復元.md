# 型07 実行ログ: PWA再表示時の状態復元

- 型選定: 型07（完成基準駆動）。保存対象と復元条件をコード上で検証できるため。
- 原因: `initApp()` が `currentView === 'detail'` を不正な画面として扱い、必ずホームへ遷移していた。検索語、現在のフィルター、スクロール位置は保存されていなかった。
- 実施: ローカル専用の復元状態を追加。詳細レシピID・戻り先・検索語・フィルター・各画面のスクロールを保存し、レシピ読込完了後に詳細を復元する。復元時は閲覧履歴を重複追加しない。画面遷移はブラウザ履歴と連携し、Androidの戻る／進むをアプリ内遷移として扱う。
- 検証: 状態正規化、詳細保存、復元ライフサイクル、ホーム→詳細→戻る→進むの履歴模擬、JavaScript構文、`git diff --check` がPASS。
- 公開状態: 未公開。既存の `docs/gas/Code.js` 変更は対象外。
- 差別化点: 単なる最終タブの復元ではなく、レシピデータが利用可能になってから詳細とスクロール位置を復元する。
- 改善候補: 今回はなし。
